services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nusra
      POSTGRES_USER: nusra
      POSTGRES_PASSWORD: nusra_local
    ports:
      - "5432:5432"
    volumes:
      - nusra_pg:/var/lib/postgresql/data
volumes:
  nusra_pg:
